# Keep secrets and large trees out of the build context / image.
# Anchored to repo root so it does NOT match the internal/config source package.
/config/
.git
.gitignore
.dockerignore
Dockerfile
README.md
examples
*.md

# local build artifacts
/build/
node_modules
web/node_modules
web/test-results
web/playwright-report
# The frontend stage generates these assets from source.
/web/dist/
/internal/ui/dist/

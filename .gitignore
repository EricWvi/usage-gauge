# Runtime config (endpoints.yaml with real keys, user parsers, gauge.db)
# Anchored to repo root so it does NOT match the internal/config source package.
/config/

# Local build artifacts
/build/

# SQLite runtime files
*.db
*.db-wal
*.db-shm

# Frontend dependencies and browser test artifacts
node_modules/
/web/dist/
# Legacy frontend output (before moving Vite output to web/dist).
/internal/ui/dist/
/web/test-results/
/web/playwright-report/
